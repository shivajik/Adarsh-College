{
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/adarsh-college run build-vercel",
  "outputDirectory": "artifacts/adarsh-college/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
